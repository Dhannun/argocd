apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.34.0 (HEAD)
  labels:
    io.kompose.service: grading
  name: grading
spec:
  type: LoadBalancer
  ports:
    - name: '80'
      port: 80
      targetPort: 2024
  selector:
    io.kompose.service: grading
