apiVersion: v1
kind: Service
metadata:
  name: sinnts-grading
  namespace: dev
  labels:
    app: grading
spec:
  type: ClusterIP
  selector:
    app: grading
  ports:
    - protocol: TCP
      port: 80
      targetPort: 2024
